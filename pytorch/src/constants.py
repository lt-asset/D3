# Batch size of input dataset
# Change to smaller sizes (e.g., 240) if storage space is limited
BATCH_SIZE = 2400

# Number of models to generate
# NUM_MODELS = 200
NUM_MODELS = 8

# Number of training inputs to the model
# NUM_TRAINING_DATASETS = 10
NUM_TRAINING_DATASETS = 1